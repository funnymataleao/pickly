project_id = "bslsanvmkwjzbjerpzpi"

[auth]
additional_redirect_urls = ["pickly://auth/reset-password"]
minimum_password_length = 8
# Hosted projects on the Free plan reject this setting. Enable it in the
# Supabase Dashboard after upgrading the project to Pro.

[functions.delete-account]
verify_jwt = true

[functions.apple-token]
verify_jwt = true
